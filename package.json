{
  "name": "progressionbuilder-demo",
  "version": "1.0.0",
  "description": "A demo application showing how to use Google's Gemini API to suggest chords for a given chord progression",
  "main": "dist/progressionbuilder-tests.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/progressionbuilder-tests.js",
    "dev": "ts-node --transpile-only progressionbuilder-tests.ts",
    "test": "npm run build && node dist/progressionbuilder-tests.js",
    "manual": "npm run build && node -e \"require('./dist/progressionbuilder-tests.js').testManualScheduling()\"",
    "llm": "npm run build && node -e \"require('./dist/progressionbuilder-tests.js').testLLMScheduling()\"",
    "mixed": "npm run build && node -e \"require('./dist/progressionbuilder-tests.js').testMixedScheduling()\""
  },
  "keywords": ["gemini", "ai", "schedule", "student", "demo", "typescript"],
  "author": "Angela Chen",
  "license": "MIT",
  "dependencies": {
    "@google/generative-ai": "^0.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  }
}
